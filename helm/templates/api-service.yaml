apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-api
  labels:
    app: {{ $.Release.Name }}-api
spec:
  selector:
    app: {{ $.Release.Name }}-api
  ports:
  - name: http
    port: 4000
    targetPort: http
    protocol: TCP
